package sexpr.matching;

import sexpr.SExpr;


/**
 * a matcher that matches only part of the data,
 * returning a XMatchResult containing the "unconsumed" data as well 
 * @author Administrator
 */
public interface XPartialMatcher {}
